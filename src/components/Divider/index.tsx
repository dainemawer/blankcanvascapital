/*
 * Divider
 *
 * Divider Component
 *
 * @returns {JSX.Element}
*/

import { FC } from 'react'

interface DividerProps { }

export const Divider: FC<DividerProps> = (): JSX.Element => {
	return (
		<p>Divider</p>
	);
}
