/*
 * Icon
 *
 * Icon Component
 *
 * @returns {JSX.Element}
*/

import { FC } from 'react'

interface IconProps { }

export const Icon: FC<IconProps> = (): JSX.Element => {
	return (
		<p>Icon</p>
	)
}
