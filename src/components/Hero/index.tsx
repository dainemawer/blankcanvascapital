/*
 * Hero
 *
 * Hero Component
 *
 * @returns {JSX.Element}
*/

import { FC } from 'react'

interface HeroProps { }

export const Hero: FC<HeroProps> = (): JSX.Element => {
	return (
		<h1>Hero</h1>
	)
}
