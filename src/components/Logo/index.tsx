/*
 * Logo
 *
 * Logo Component
 *
 * @returns {JSX.Element}
*/

import { FC } from 'react'

interface LogoProps { }

export const Logo: FC<LogoProps> = (): JSX.Element => {
	return (
		<p>Logo</p>
	)
}
