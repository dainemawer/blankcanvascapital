/*
 * Address
 *
 * Address Component
 *
 * @returns {JSX.Element}
*/

import { FC } from 'react'

interface AddresssProps {}

export const Address: FC<AddresssProps> = (): JSX.Element => {
	return (
		<address>Address</address>
	);
}
