/*
 * Form
 *
 * Contact Form
 *
 * @returns {JSX.Element}
*/

import { FC } from 'react'

interface FormProps { }

export const Form: FC<FormProps> = (): JSX.Element => {
	return (
		<form>Form</form>
	)
}
